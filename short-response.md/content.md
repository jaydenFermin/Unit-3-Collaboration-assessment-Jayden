1 The first step both teammates need to do is to create a branch and its important to create the branch so they can work together.

2 Developers use branches so they could work on a repo without causing issues.

3 Git is a command line tool used to keep track of files that are being edited and github is a cloud based services that allows developers to store, track, manage, and control changes to their code. Git and github work together because git is a tool used inside of github.     

4 The command sequence is wrong because git add is supposed to go first and then its git commit so its suppose to look like 
git add .
git commit -m "saving work"
git push








